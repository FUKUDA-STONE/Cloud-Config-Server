server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:D:\JOB\Fukuda.AI\fukuda_stone_alva_api_v1
          clone-on-start: true
